import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: { flex: 1 },
  inputTitle: { fontWeight: "bold", paddingVertical: 8, color: "white" },
  errorText: { color: "red", fontSize: 12 },
  textInput: { color: "white" },
});
